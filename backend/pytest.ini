[pytest]
# `pythonpath = .` lets tests do `from app.main import app` when pytest is run
# from backend/, without needing an installed package or PYTHONPATH juggling.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
